import * as React from 'react';
import './App.css';
import {
    Update
} from "./layouts";

export default (props: any) => {
    return (
        <div className="App">
            <Update />
        </div>
    );
};